[project]
name = "smart-timetable"
version = "0.1.0"
description = "Smart Timetable Planner to generate conflict-free course schedules"
authors = [{name = "AutoGenerated"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
smart-timetable = "smart_timetable.cli:main"

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-q"
